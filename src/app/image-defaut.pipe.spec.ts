import { ImageDefautPipe } from './image-defaut.pipe';

describe('ImageDefautPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageDefautPipe();
    expect(pipe).toBeTruthy();
  });
});
